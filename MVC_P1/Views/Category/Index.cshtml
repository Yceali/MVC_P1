@model IEnumerable<MVC_P1.Models.DBModels.Kategoriler>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.KategoriAdi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tanimi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Resim)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.KategoriAdi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tanimi)
        </td>
        <td>
            @if (item.Resim == null)
            {
                <text>Resim Yok</text>
            }
            else
            {
                <text>Resim Var</text>
            }
            @*@Html.DisplayFor(modelItem => item.Resim)*@
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.KategoriID }) |
            @Html.ActionLink("Details", "Details", new { id=item.KategoriID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.KategoriID })
        </td>
    </tr>
}

</table>
